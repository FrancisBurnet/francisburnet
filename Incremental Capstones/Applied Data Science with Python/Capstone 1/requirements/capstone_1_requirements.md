# Capstone 1 Requirements

Source directions file: `Capstone_Session_1.pdf`
Reference notebook used for cross-checking requirement order: `capstone_1.ipynb`

## 1. Data Import and Cleaning
Source of truth: PDF task bullets on pages 15-16

1a. Import relevant Python libraries necessary for Python programming and Numpy for numerical operations.
Status: The copied notebook documents a `pandas` import. A separate `numpy` import is not surfaced in the copied notebook.

1b. Import the CSV file `NSMES1988.csv` into a dataframe.
Status: Supported by the copied notebook load step.

1c. Inspect the dataset and report rows, columns, and data types.
Status: Supported by copied notebook inspection and profiling cells.

1d. Find out if the data is clean or if the data has missing values.
Status: Supported by copied notebook missing-value checks.

1e. Comment on the data types, their values and range, specifically on `age` and `income` columns.
Status: Supported by copied notebook age/income commentary.

1f. Export the data to JSON as `NSMES1988.json` and view and enter your comments.
Status: Supported by copied notebook JSON export and preview.

1g. Perform memory information on the data and recommend what non-default data types would optimize dataframe memory settings.
Status: Supported by copied notebook memory-usage and dtype recommendation section.

1h. Recommend what changes should be made on the dataframe before attempting a detailed data analysis.
Status: Supported by copied notebook recommendation to drop the index-like `Unnamed: 0` column.

1i. Export the dataframe as a new CSV file `NSMES1988new.csv` and store it locally for other assignments.
Status: Supported by copied notebook cleaned CSV export.

1j. Write a short report on the visual observations of the data.
Status: This PDF requirement is not yet surfaced as a dedicated visual-observations report in the copied notebook or current website page.

## Requirements Extraction Counts
- Actionable PDF requirement items extracted from the copied Capstone 1 directions file: 10
- Notebook-backed items currently surfaced on the website: 9
- PDF-only item still awaiting source-backed notebook/page evidence: 1 (`1j` visual observations report)